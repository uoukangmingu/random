# 랜덤 룰렛 성능 최적화 분석

## 확인된 핵심 병목

1. `style.css` 약 726KB, `script.js` 약 582KB가 첫 실행에 전부 해석됐다.
2. 홈 화면에서도 해전·주식·사다리·풍선 등 숨겨진 게임 UI를 미리 만들었다.
3. 화면을 한 번 이동할 때마다 현재 화면과 관계없는 게임까지 초기화하고 DOM을 다시 그렸다.
4. 담아라!는 화면을 떠난 뒤에도 Matter.js 렌더러와 러너가 계속 동작할 수 있었다.
5. 담아라!의 구슬 집계 UI가 물리 업데이트마다 최대 초당 60회 다시 만들어졌다.
6. 게임 시작 버튼 상태 확인이 화면과 무관하게 180ms마다 반복됐다.
7. 주식게임은 250ms마다 여러 목록과 차트를 통째로 다시 만들었다.
8. 곰찾기 영상 두 개가 각각 약 19MB·1440×1440이었고, 포스터도 약 2.9MB였다.
9. 블러, 백드롭 블러, 다중 그림자, 배경 장식, 지옥 모드 합성 레이어가 모바일 GPU의 합성 비용을 높였다.
10. 볼 배틀의 `afterUpdate` 이벤트 객체가 강제 갱신 요청처럼 처리되어 HP 라벨이 물리 프레임마다 다시 배치됐다.
11. 볼 배틀 안전 구역이 물리 프레임마다 여러 DOM 스타일을 쓰고 화면 크기를 다시 읽었다.
12. 공이 충돌할 때마다 랭킹 목록 전체 HTML을 재생성하고, 피격 애니메이션 재시작을 위해 강제 레이아웃을 발생시켰다.

## 기기별 적용값

| 항목 | 일반 PC | 저사양 사무용 PC | 모바일·태블릿 |
|---|---:|---:|---:|
| 물리 목표 갱신 | 50Hz | 50Hz | 50Hz |
| 경마 화면 계산 상한 | 60fps | 40fps | 30fps |
| 캔버스 픽셀 비율 상한 | 1.25 | 1.0 | 0.8~0.9 |
| 담아라! 구슬 수(5그릇 이하) | 220 | 220 | 220 |
| 담아라! 집계 UI 갱신 | 최대 12.5fps | 최대 8.3fps | 최대 5fps |
| 주식 계산 / 화면 갱신 | 250ms / 250ms | 250ms / 400ms | 250ms / 500ms |
| 폭발 입자 수 | 100% | 약 70% | 약 50% |
| 장식 블러·백드롭 블러 | 유지 | 제거 | 제거 |
| 배경 장식·진입 페이드 | 유지 | 테마 배경 유지·장식 애니메이션 정지 | 테마 배경 유지·장식 애니메이션 정지 |

### 볼 배틀 전용 적용값

| 항목 | 일반 PC | 저사양 사무용 PC | 모바일·태블릿 |
|---|---:|---:|---:|
| 캔버스 렌더링 상한 | 약 60fps | 약 30fps | 약 30fps |
| HP 라벨·안전 구역 목표 갱신 | 30fps | 25fps | 20fps |
| 캔버스 픽셀 비율 상한 | 1.1 | 0.85 | 0.7 |
| 충돌 랭킹 갱신 간격 | 140ms | 280ms | 420ms |
| 동시 일회성 DOM 효과 상한 | 10개 | 5개 | 3개 |
| 떠오르는 데미지 숫자 | 사용 | 제한 사용 | 미사용 |
| 라벨 블러·큰 폭발 그림자 | 사용 | 제거 | 제거 |

저사양·일반 사무용 PC는 `deviceMemory` 4GB 이하, 논리 CPU 8개 이하 또는 데이터 절약 모드를 기준으로 자동 판정한다. 값을 제공하지 않는 브라우저에서는 일반 PC 설정을 사용한다.

## 리소스 경량화 결과

| 파일 | 이전 | 최적화 후 | 감소 |
|---|---:|---:|---:|
| 곰 영상 PC | 19.25MB | 1.06MB | 약 94.5% |
| 판다 영상 PC | 19.59MB | 1.13MB | 약 94.2% |
| 곰 영상 모바일 | 19.25MB 기준 | 0.47MB | 약 97.5% |
| 판다 영상 모바일 | 19.59MB 기준 | 0.50MB | 약 97.4% |
| 시작 포스터 | 2.93MB PNG | 65KB WebP | 약 97.8% |
| 실행 JS | 588KB 원본 | 422KB 압축본 | 약 28% |
| 실행 CSS | 730KB 원본 | 약 567KB 압축본 | 약 22% |

영상은 H.264/AAC와 `faststart`를 유지해 일반 사무용 PC, Android, iPhone에서 하드웨어 디코딩이 가능한 구성을 사용했다. 모바일은 540×540, PC는 720×720이며 실제 표시 크기에는 충분하다.

## 동작 구조 변경

- 홈에서는 각 게임의 입력값만 준비하고 복잡한 보드 DOM은 해당 게임에 들어갈 때 생성한다.
- 화면 이동 시 방금 떠난 게임만 정리한다. 다른 숨은 게임을 매번 다시 렌더링하지 않는다.
- 담아라! 물리엔진은 화면 이탈과 백그라운드 탭에서 완전히 멈추고 재진입 시 다시 켠다.
- 모바일·사무용 PC에서는 전체 DOM 이모지 지원 검사를 생략하고, 고성능 PC에서도 최초 유휴 시간에 한 번만 검사한다. 마우스가 있는 환경에서는 성능 등급과 관계없이 커스텀 커서를 사용한다.
- 곰찾기 영상은 상자를 누른 순간에만 소스를 지정하며, 모바일은 별도 경량 영상을 선택한다.
- 외부 Matter.js CDN 요청을 제거하고 같은 서버의 로컬 파일을 사용한다.
- 담아라!와 볼 배틀의 물리 판정은 모든 기기에서 50Hz 고정 시간축을 사용한다. 캔버스 해상도와 화면 그리기만 기기 성능에 맞게 제한한다.
- 주식게임은 모든 기기에서 250ms마다 같은 가격 계산을 수행하고, DOM 차트 갱신만 250~500ms로 조절한다.
- 볼 배틀 HP 라벨과 안전 구역은 캐시한 화면 배율을 사용하고, 실제 크기 변경이나 확대 시에만 레이아웃 크기를 다시 측정한다.
- 랭킹은 충돌마다 즉시 전체 재생성하지 않고 짧은 간격으로 합쳐서 처리한다. 탈락과 최종 결과는 즉시 반영한다.
- 전투가 끝나면 Matter.js 러너와 연속 캔버스 렌더링을 즉시 멈춘다. 종료 화면에서 테마를 바꿀 때만 캔버스를 한 번 다시 그린다.

## 유지된 기능

룰렛 결과, 참가자 순서, 당첨 판정, 카드·사다리·경마·주식게임 규칙은 유지했다. 성능 등급별 차이는 캔버스 해상도, 화면 표현량과 렌더링 빈도에만 적용하며 게임 계산 횟수와 구슬 수는 기기와 무관하게 동일하다.
