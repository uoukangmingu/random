# 랜덤 룰렛 v3.27 변경·검증 기록

기준: v3.26 SPIN/STOP·음악 업데이트본. 기존 낮·밤 테마, 둥근 카드, 손글씨 제목과 게임 규칙을 유지했습니다.

## 수정 사항

- 메뉴·홈의 오른쪽 전용 여백을 같은 크기의 좌우 여백으로 변경했습니다. PC·태블릿의 설정·이전 버튼은 세로로 배치해 카드 영역과 분리했습니다. 모바일 메뉴의 가로 넘김은 유지합니다.
- 담아라!에서 메뉴가 한 번 숨겨진 뒤 처음 게임에 들어가면 낙하 타이머만 정지 상태로 남던 오류를 수정했습니다. 최초 초기화·진입·시작에서 물리 엔진과 타이머를 함께 재개합니다.
- 시작 클릭에서 첫 공을 바로 생성합니다. 시작·리셋 시 버튼 상태를 즉시 갱신하고 중복 시작을 막습니다. 리셋·게임 이탈은 예약된 공을 취소합니다. 완료된 공 생성 타이머를 계속 보관하지 않습니다.
- 원판에 회전 속도 배율 입력을 추가했습니다. 0.5~10배, 기본 1배이며 소수 입력도 가능합니다. 유효하지 않은 값은 안내하고 시작을 막습니다. 유효한 설정은 브라우저에 저장하며 다음 SPIN에 적용합니다.
- 회전 중 속도 편집은 잠그고 STOP은 계속 사용할 수 있습니다. STOP 후 약 4.8초 감속·가중치 추첨·결과 기록은 유지합니다. 기기의 모션 감소 설정에서는 기존 느린 회전을 유지하고 화면에 이를 안내합니다.

## 원인 재현과 자동 검사

기존 v3.26 ZIP의 실제 script.js와 동봉한 Matter.js를 사용한 모의 실행에서, 메뉴가 숨겨진 뒤 복귀하여 첫 진입·시작을 수행하면 물리 시간은 약 5초 진행하지만 공은 **0개**였습니다. 정지된 타이머를 재개하지 않는 초기화 경로를 확인했습니다.

수정본은 같은 조건에서 클릭 즉시 첫 공이 생성됩니다. 60Hz 모의 프레임과 실제 Matter.js 물리를 사용했습니다.

| 보드 크기 | 품질 | 시작 5초 뒤 그릇에 도착한 일반 구슬 |
| --- | --- | ---: |
| 760×428 | 균형 | 105개 |
| 320×380 | 성능 우선 | 37개 |
| 1100×619 | 고화질 | 75개 |

세 조건 모두 220개 일반 구슬+20개 폭탄이 중복 없이 생성됐습니다. 중간에 탭을 60초 숨겨도 공 생성 수·물리 시간이 진행하지 않았고 복귀 후 남은 낙하가 이어졌습니다. 중복 재개가 물리 루프를 복제하지 않았습니다. 리셋 후 공 0개, 재진입 후 다시 낙하, 종료 후 남은 타이머 0개를 확인했습니다.

원판 30fps 모의 실행에서 0.5배=초당 7회전, 2배=28회전, 2.75배=38.5회전, 10배=140회전을 확인했습니다. 20초 동안 STOP 전 자동 종료하지 않고 STOP 후 감속·결과 저장은 한 번씩만 발생했습니다. 빈 값·0·음수·범위 초과·비정상 숫자의 실행을 막고 새 초기화에서 저장한 10배 설정이 복원됐습니다. 모든 배율에서 항목 가중치 값은 유지됐습니다.

빌드 및 전체 `npm run check`를 통과했습니다. 원판·카드 자유 공개·음악 스케줄러·모바일 메뉴 순환·참가자 조건·입력 저장·비활성 탭 처리·경마·풍선·반응 게임의 기존 검사도 통과했습니다. 버튼 상태 즉시 갱신 추가 후 다시 빌드하고 구슬 회귀 검사를 통과했습니다.

## 브라우저 확인

- 1353px 가용 폭의 메뉴에서 기존 중심 오차 −56.375px가 수정 후 0px가 됐습니다. 좌우 여백은 약 68.15px로 동일합니다.
- 담아라! 시작 직후 `구슬이 떨어지는 중... 1/240`, 이후 전체 투하 및 세 그릇의 실제 집계 증가를 확인했습니다. 리셋 확인 후 구슬과 집계가 0개로 돌아왔습니다.
- 원판에 3을 입력하면 현재 3배 안내가 나타나고 기존 밤 테마와 조화롭게 표시됩니다. SPIN 후 두 버튼 모두 STOP으로 전환되고 속도 입력은 잠겼습니다. STOP 후 감속·치킨 당첨·기록 4→5개·SPIN과 입력 조작 복원을 확인했습니다.

## 확인 범위

브라우저 화면·조작은 데스크톱 Chrome에서 확인했습니다. 위 초당 회전수와 낙하 시간은 실제 소스·Matter.js를 실행한 모의 시계 검사 수치이며 실기기의 화면 FPS 측정값이 아닙니다. iPhone·Android 실기기 터치 및 모든 게임의 전체 라운드를 이번에 재검증한 것은 아닙니다.
