# 랜덤 룰렛 v3.28

기존 낮·밤 배경, 색감, 둥근 카드, 손글씨 제목과 13개 게임 구성을 유지한 게임플레이 개선본입니다. 이번 버전은 원판의 속도 배율이 무시되는 문제를 수정하고 회전 중 배율 변경과 고속 회전 잔상을 추가했습니다. v3.27의 메뉴 중앙 정렬과 담아라! 시작·복귀 수정도 포함합니다.

## 이번 업데이트

| 항목 | 바뀐 동작 |
| --- | --- |
| 메뉴 정렬 | PC·태블릿 메뉴와 홈에 같은 크기의 좌우 여백을 적용했습니다. 빠른 조작 버튼은 옆의 세로 열에 놓여 게임 카드와 겹치지 않습니다. |
| 담아라! | 다른 탭에서 돌아온 뒤 처음 실행할 때 공이 생성되지 않는 타이머 재개 오류를 수정했습니다. 시작 즉시 첫 공·진행 상태가 나타나며 리셋·화면 복귀 후 다시 실행할 수 있습니다. |
| 속도 배율 | 회전 속도 배율에 0.5~10 사이의 숫자를 직접 입력합니다. 기본 1배, 2 입력 시 2배 속도입니다. 입력값을 저장하며 회전 중에도 즉시 적용합니다. STOP 이후 감속 중에는 속도 입력을 잠급니다. |
| 원판 룰렛 | 기본 1배에서 SPIN을 누르면 약 0.26초 동안 가속한 뒤 초당 14바퀴 속도로 계속 회전합니다. 같은 버튼이 STOP으로 바뀝니다. STOP을 누르면 약 4.8초 동안 서서히 멈춰 당첨 위치에 정렬합니다. |
| 카드 연산 배틀 | 전원의 첫 3장 계산을 마친 뒤, 남은 두 장은 연산 기호·숫자 중 원하는 것부터 공개할 수 있습니다. 참가자마다 다른 순서로 열어도 됩니다. |
| 음악 | 메인·메뉴와 13개 게임에 각각 새로운 8마디 음악을 연결했습니다. 멜로디·베이스·화음·타악기를 조합하며 화면 이동 때 부드럽게 전환됩니다. |

### 원판 조작

항목 설정 영역의 **회전 속도 배율**에 숫자를 입력합니다. 1배는 초당 14바퀴, 2배는 28바퀴, 10배는 140바퀴의 계산상 회전 속도입니다. 2.5처럼 소수도 사용할 수 있습니다. 속도는 시작 전과 회전 중 모두 조절할 수 있고, STOP 후에는 배율에 맞는 회전 거리를 두고 약 4.8초 동안 감속합니다. 속도 설정은 당첨 확률에 영향을 주지 않습니다. 빈 값이나 범위를 벗어난 값은 안내하고 시작을 막습니다. 모바일에서는 원판 아래 **항목 · 속도 설정**을 누르면 설정 영역으로 이동합니다.

중앙 SPIN과 아래 SPIN 버튼은 같은 역할입니다. 회전이 시작되면 둘 다 STOP으로 바뀌며 어느 쪽을 눌러도 감속합니다. 감속 중에는 추가 입력을 막고, 결과가 나오면 SPIN으로 돌아옵니다. 자동 종료 시간은 없습니다. 바로 STOP을 누르더라도 짧은 가속을 마치고 감속합니다.

추첨 가중치와 결과 기록·재추첨·당첨자 제외 기능은 유지했습니다. STOP은 감속 시점을 정하며 결과는 등록한 가중치에 따라 무작위로 결정됩니다. 화면을 벗어나 종료하면 취소된 추첨을 기록하지 않습니다. 다른 탭으로 이동하면 회전 시간이 일시 정지됩니다.

원판의 속도는 기기의 모션 감소 설정과 관계없이 입력한 배율을 따릅니다. 기기의 모션 감소 설정은 배경·결과 장식 애니메이션에 계속 적용됩니다. v3.27에서 모든 배율을 초당 0.6바퀴로 고정하던 경로를 수정했습니다.

회전 중 배율을 변경하면 약 0.26초 동안 새 속도로 부드럽게 전환합니다. 각도가 갑자기 튀거나 추첨이 재시작되지 않습니다. 잘못된 입력을 편집하는 동안에도 STOP을 사용할 수 있습니다. 고속에서는 각도에 따른 회전 잔상을 표시하고, 감속하면 원래의 선명한 원판으로 돌아옵니다. 잔상 이미지는 최대 512×512 크기 5단계를 필요할 때 한 번씩 생성해 재사용하며 매 프레임 다시 그리지 않습니다.

### 음악 테마

| 화면·게임 | 음악 방향 |
| --- | --- |
| 메인 | 부드러운 건반과 따뜻한 화음 |
| 게임 메뉴 | 가벼운 라운지 리듬과 말렛 |
| 원판 룰렛 | 빠른 신스 아르페지오와 댄스 리듬 |
| 담아라! | 통통 튀는 말렛·나무 타악기 |
| 경마 | 질주를 닮은 타악기와 브라스 신스 |
| 카드 배틀 | 스윙 리듬의 건반·베이스 |
| 볼 배틀 | 낮은 신스와 아케이드 드럼 |
| 러시안 룰렛 | 낮은 심박 리듬과 드문 플럭 음 |
| 주식게임 | 빠르게 움직이는 전자음 시퀀스 |
| 투명 사다리 | 맑은 벨과 부드러운 화음 |
| 풍선 | 짧은 현악 느낌의 플럭과 나무 타악기 |
| 폭탄 돌리기 | 시계 같은 타악기와 낮은 베이스 |
| 원 안 터치 | 간결하고 또렷한 플럭 리듬 |
| STAY CLICK | 준비 화면의 잔잔한 벨·화음. 실제 라운드 중 배경음은 조용해져 신호에 집중 가능 |
| 곰찾기 | 장난감 가게 느낌의 벨. 영상 재생 중에는 배경음이 잦아듦 |

낮·밤은 같은 곡을 공유하면서 밤에 템포를 6% 낮추고 타악기와 고음을 부드럽게 조정합니다. 룰렛 감속·경마 진행·카드 후반에는 리듬의 강도가 변합니다. 반응 게임이나 곰찾기 영상에서는 효과음·영상 소리가 들리도록 배경음을 낮춥니다. 음량 설정과 음소거는 그대로 사용할 수 있습니다.

곡은 이 프로젝트용으로 작성한 악보를 브라우저에서 합성합니다. 외부 음악 다운로드는 없으며 별도의 음원 파일을 불러오지 않습니다. 브라우저 정책에 따라 처음 클릭·터치한 뒤 소리가 시작됩니다.

## 유지한 개선

- 원판은 프레임 기반 transform을 사용합니다. 회전 중에는 원판과 저장한 잔상 이미지의 각도를 바꾸며, 잔상 강도가 달라질 때만 필요한 이미지를 교체합니다.
- PC는 입력과 원판의 2열, 작은 화면은 원판과 조작부를 먼저 보여줍니다. 항목 수정 바로가기, 접이식 기록, 재추첨·당첨자 제외 기능을 유지했습니다.
- 당첨자를 제외해도 다음 추첨·편집 전까지 직전 원판과 결과를 유지합니다. 남은 항목이 2개면 제거할 수 없습니다.
- 공용 목록 저장·불러오기, 잘못된 가중치·중복 입력 처리, 결과·설정·입력 초안 저장을 유지했습니다.
- 배경음과 효과음 슬라이더, 키보드 포커스, 터치 영역과 첫 로딩의 중복 실행·재시도 처리를 유지했습니다.
- 게임 코드 압축, 첫 화면의 지연 로딩, 캔버스 해상도 조절, 비활성 화면의 불필요한 렌더링 억제를 유지했습니다.

## 실행

ZIP을 풀어 최상위 폴더의 내용을 정적 웹 호스팅 또는 GitHub Pages에 올리면 됩니다. 실행 파일과 자산을 모두 포함했으며 서버 프로그램이나 데이터베이스는 필요하지 않습니다.

기존 `CNAME`, QR 이미지, 앱 아이콘과 동영상도 포함했습니다. 다른 도메인을 사용할 때는 CNAME과 QR의 연결 주소를 함께 바꾸세요.

로컬 확인:

```sh
npm ci
npm run dev
```

앱 설치와 화면 꺼짐 방지 기능은 이를 지원하는 HTTPS 환경에서 사용하세요.

## 소스 수정·재빌드

```sh
npm ci
npm run build
npm run check
```

빌드는 최상위 실행 파일과 `dist/`를 갱신합니다. `npm run check`는 정적 검사와 Node 모의 실행 검사를 수행합니다. 브라우저 자동 검사는 포함하지 않습니다.

| 수정 대상 | 원본 파일 |
| --- | --- |
| 기존 게임과 공통 처리 | `script.js` |
| 원판 룰렛 | `src/games/wheel.js` |
| 새 게임 규칙 계산 | `src/shared/gameplay-rules.js` |
| 음악 악보·악기·재생기 | `src/shared/soundtrack.js` |
| 게임 목록 조건 | `src/games/registry.js` |
| 화면 HTML | `src/index.template.html` |
| 기존 디자인 | `style.css` |
| 공용 기능 디자인 | `src/styles/features.css` |
| 디자인 보완·새 단계 표시 | `src/styles/polish.css` |
| 공용 목록·설정·로딩 | `src/shared/` |

`src/games/`의 core, bootstrap, game1~7, physical-games는 `script.js`에서 자동 분리되므로 직접 수정하지 마세요. 최상위 index, shell, runtime, app-markup도 생성 파일입니다. 빌드가 만드는 v3.21 이름의 번들은 기존 검사와의 호환용이며 실제 화면은 v3.28 파일을 사용합니다.

배포용 ZIP에서는 이전 실행 파일, 중복 호환 번들, node_modules, dist와 임시 검사용 화면을 제외했습니다. 편집 원본과 빌드·검증 스크립트는 포함했습니다. 검사를 다시 실행하려면 먼저 위 순서대로 빌드하세요.

## 확인 범위와 저장

공용 목록·입력·기록·설정은 현재 브라우저에 저장됩니다. 브라우저 데이터를 지우면 함께 삭제되며 여러 기기 사이에 자동 동기화되지 않습니다.

새 서비스 워커는 진행 중인 탭을 강제로 교체하지 않고 기존 탭이 닫힌 뒤 활성화됩니다. 아직 열지 않은 게임은 오프라인에서 실행이 제한될 수 있습니다.

이번 버전의 확인 범위는 `QA_REPORT_v3.28.md`, 파일 용량은 `PERFORMANCE_COMPARISON.json`을 확인하세요. 실제 iPhone·Android의 터치와 기기별 FPS는 측정하지 않았습니다.
