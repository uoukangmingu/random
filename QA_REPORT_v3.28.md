# 랜덤 룰렛 v3.28 속도 적용 수정

확인일: 2026-09-10. 기준: v3.27.

## 원인

v3.27의 `getSpinMotionProfile()`은 기기의 `prefers-reduced-motion`이 켜졌을 때 모든 배율을 초당 0.6회전으로 덮어썼습니다. 이 조건에서 사용자가 배율을 입력해도 실제 속도는 바뀌지 않았습니다. 이전의 일반 모드 검사만으로는 이 오류를 걸러내지 못했습니다.

기존 ZIP의 실제 원판 소스와 수정본을 같은 조건으로 실행한 결과입니다.

| 입력 배율 | 기존 v3.27, 모션 감소 켜짐 | 수정 v3.28, 모션 감소 켜짐 |
| --- | ---: | ---: |
| 1배 | 초당 0.6회전 | 초당 14회전 |
| 3배 | 초당 0.6회전 | 초당 42회전 |
| 10배 | 초당 0.6회전 | 초당 140회전 |

사용자의 기기 설정을 직접 읽은 것은 아닙니다. 위 오류 경로를 실제 코드로 재현하고 수정했습니다.

## 변경

- 원판의 명시적인 속도 배율을 기기의 장식 모션 설정이 덮어쓰지 않게 했습니다. 배경·결과 장식의 모션 감소 CSS는 유지됩니다.
- 회전 중에도 속도 입력을 사용할 수 있고 변경한 배율이 바로 반영됩니다. 약 0.26초 동안 현재 속도에서 새 속도로 전환하며 각도가 튀거나 추첨이 새로 시작되지 않습니다.
- STOP 이후에는 현재 속도를 기준으로 약 4.8초 동안 감속합니다. 감속 중에는 배율을 잠그고, 종료 후 입력을 다시 엽니다.
- 입력을 잠시 지우거나 잘못 입력해도 이미 회전 중인 원판의 STOP은 사용할 수 있습니다. 유효한 값이 들어오면 적용합니다.
- 고속 회전에는 각도 방향 잔상을 표시합니다. 빠른 원판이 선명한 정지 화면의 연속처럼 보이는 현상을 줄입니다. 감속하면서 잔상이 줄고 결과에서는 원래 선명한 원판으로 돌아옵니다.
- 잔상은 최대 512×512 크기 5단계를 필요한 때 생성하고 한 회전 동안 재사용합니다. 같은 속도가 유지되는 프레임에는 다시 그리지 않습니다. 종료·취소 시 임시 이미지를 정리합니다.
- 메뉴 중앙 정렬, 담아라! 시작·복귀 수정, 카드 배틀 공개 순서와 기존 음악을 유지했습니다.

## 검사

빌드와 전체 `npm run check`를 통과했습니다.

- 일반 모드와 모션 감소 모드에서 설정한 배율을 따릅니다. 모션 감소 모드의 실제 실행에서 3배→10배 전환을 확인했습니다.
- 0.5·2·2.75·10배의 속도, 회전 중 1→10→2배 변경, 변경 순간의 각도 연속성과 결과 1회 저장을 확인했습니다.
- 잔상 캔버스가 기본 원판과 같은 transform을 사용하며, 안정된 속도의 프레임에는 이미지 생성·다시 그리기 0회였습니다. 감속 종료 후 기본 원판이 복원되고 임시 캔버스가 정리됐습니다.
- 20초 동안 STOP 전 자동 종료하지 않았고, 중복 STOP·취소 후 결과 생성 방지·숨겨진 탭 시간 제외·재시작을 확인했습니다.
- 잘못된 속도 값과 빈 값은 새 시작을 막고, 회전 중 편집할 때 STOP 조작은 유지됐습니다.
- 기존 담아라!·카드 배틀·경마·풍선·반응 게임·음악·공용 목록·모바일 메뉴의 회귀 검사도 통과했습니다.

초당 회전수는 소스의 각도 이동량입니다. 디스플레이 주사율이나 사용자 기기의 실제 FPS를 뜻하지 않습니다. 실기기 iPhone·Android 및 사용자의 PC에서 직접 실행한 검사는 포함하지 않습니다.

## 실제 브라우저 확인

데스크톱 Chrome에서 같은 추첨을 유지하며 1배→10배로 변경했습니다. 입력창은 회전 중에도 사용 가능했고, 각각 다른 강도의 회전 잔상과 CSS 회전 행렬을 확인했습니다. 화면에 표시되는 잔상 캔버스의 DOM 각도 값을 시간 간격을 두고 읽어 1배에서 약 14.2회전/초, 10배에서 약 140.0회전/초를 확인했습니다. 측정에는 브라우저 조작 도구의 통신 지연이 포함되므로 정확한 프레임 성능 수치는 아닙니다.

10배에서 STOP을 눌러 감속한 뒤 치킨 당첨, 추첨 기록 5→6개, SPIN·속도 입력 복원을 확인했습니다. 로컬 앱 URL의 브라우저 오류 로그는 없었습니다.
