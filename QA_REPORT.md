# Random Roulette QA Report

## 2026-08 게임 목록 적격성·기기별 UX 개편

- `현재 명단 실행 불가`를 카드 하단의 작은 회색 라벨에서 카드 정중앙의 고대비 안내 패널로 변경했다. PC와 모바일 측정에서 패널 중심과 카드 중심의 X/Y 오차는 모두 0px였다.
- `랜덤 게임 뽑기`는 기존 요구대로 항상 첫 번째에 유지하고, 나머지는 `실행 가능 → 현재 명단 실행 불가` 순서로 자동 정렬한다. 명단을 저장할 때마다 데스크톱 그리드와 모바일 무한 캐러셀을 함께 다시 만든다.
- PC에서는 `폭탄 넘기기`, `작아지는 원` 같은 모바일 전용 게임을 목록·캐러셀·페이지 표시에서 완전히 제외한다. 모바일에서는 키보드가 필요한 `STAY CLICK`을 숨긴다.
- 터치 화면이 있는 Windows 노트북은 모바일로 오인하지 않도록 주 포인터가 coarse이고 hover가 없을 때만 휴대 기기로 판정한다.
- 목록 상단에 `바로 실행`, `명단 조정 필요`, `기기 전용 숨김` 개수를 요약하고 `참가자 명단 수정` 버튼을 추가했다.
- 모바일에서는 13개의 작은 페이지 점을 숨기고 `현재 번호 / 전체 게임 수`를 표시한다. 480px 이하에서는 카드 폭을 넓히고 명단 수정 버튼의 높이를 48px로 확보했다.
- 모바일 사다리 카드의 표시 인원은 2~5명이었지만 내부 적격성 제한이 4명이던 불일치를 5명으로 수정했다.
- PC 9인 명단에서 실행 가능 4개 뒤로 불가 7개가 정렬되고 모바일 전용 2개가 숨겨지는 것을 확인했다.
- 모바일 5인 명단에서 실행 가능 11개 뒤로 주식게임 1개만 이동하고, PC 전용 1개가 숨겨지는 것을 확인했다.
- PC·375px 모바일 모두 수평 오버플로와 앱 스크립트 오류가 0건이었다.

## 2026-08 기본 원판 룰렛 모바일·태블릿 UI 검수

- 320px·375px 휴대폰과 768px·1024px 태블릿 폭에서 원판, 중앙 SPIN, 설정, 기록 영역의 좌우 경계를 측정했다. 네 환경 모두 문서 수평 오버플로와 화면 밖 요소가 0건이었다.
- 1024px 이하에서는 `원판 → 설정 → 추첨 기록` 순서의 단일 열로 바꿔 처음 진입하자마자 원판을 볼 수 있게 했다. 원판은 컨테이너 폭을 넘지 않으며 320px 환경에서도 220px 정사각형을 유지한다.
- 휴대폰에서는 조작 버튼을 최소 48px로 키우고, 480px 이하에서 룰렛 동작 버튼을 한 열로 배치했다. 긴 항목명·결과·기록은 줄바꿈되며 기록 시간은 좁은 폭에서 다음 줄로 이동한다.
- 항목 입력 글자를 16px로 고정해 iOS 입력 확대를 막고, 입력 높이를 동적 뷰포트 기준 168~220px로 제한했다.
- 게임 목록을 스크롤한 뒤 게임에 들어가도 이전 스크롤 위치가 남지 않도록 모든 화면 전환에서 상단 위치를 복원한다.
- 원판 룰렛을 320px에서 실제 추첨해 결과와 기록이 갱신되고 SPIN 버튼이 다시 활성화되는 것을 확인했다.

## 2026-08 중복 복귀 버튼 제거·전체 게임 회귀 점검

- 전역 `이전으로` 버튼과 중복되던 담아라!, 카드 연산 배틀, 볼 배틀, 폭탄 넘기기, 작아지는 원의 내부 목록 복귀 버튼 5개를 제거했다.
- `피지컬 목록으로`, `운 게임 목록으로`, `랜덤 게임 목록으로` 문구와 `.back-btn` 요소가 HTML에 남지 않도록 자동 검사를 추가했다.
- 숨겨진 내부 뒤로가기 버튼의 존재 여부에 의존하던 담아라! 모바일 레이아웃 초기화 조건을 수정했다.
- 14개 원본 게임 카드, 첫 카드의 `랜덤 게임 뽑기`, 공용 이전 버튼 1개, 모든 HTML 버튼의 `type="button"`, 모든 ID 버튼의 실행 코드 연결을 정적 검사했다.
- 기본 룰렛과 운 게임 7종, 데스크톱 실행 가능 피지컬 게임의 진입·복귀 및 수평 오버플로를 재검사했다. 모바일 전용 2종은 PC에서 실행 불가 안내가 나타나는 경로와 모바일 레이아웃 코드를 함께 확인했다.

## 2026-08 통합 랜덤 게임 목록

- 시작 후 피지컬/운 분류 메뉴를 거치지 않고 `랜덤 게임 목록`으로 바로 이동하도록 변경했다.
- 랜덤 게임 뽑기를 첫 카드에 두고 기본 룰렛·운 게임 7개·피지컬 게임 5개를 같은 목록에 통합했다.
- 모든 14개 카드 우측 상단에 `⏩ 가능/불가` 배지를 표시하고, 기기 호환 배지는 피지컬 카드 좌측 상단으로 분리했다.
- 랜덤 게임 뽑기의 후보를 운 게임에서 현재 명단·기기로 실행 가능한 전체 운/피지컬 게임으로 확장했다.
- 실제 브라우저에서 첫 카드·14개 카드·배지 14개, 경마와 STAY CLICK 직접 진입, 통합 목록 복귀를 확인했다.
- 랜덤 게임 뽑기가 피지컬 게임인 풍선 불기를 선택하는 것을 확인했고 앱 스크립트 오류는 0건이었다.

## 2026-08 참가자 명단 입력 커서 수정

- 공용 참가자 명단 팝업이 커스텀 커서보다 높은 레이어에 있어 포인터가 가려지던 문제를 수정했다.
- 텍스트 입력창·텍스트 영역·편집 가능 요소 위에서는 운영체제 기본 I-beam 커서를 사용한다.
- 커스텀 커서는 팝업 버튼을 포함한 비입력 영역에서 팝업보다 위에 표시된다.
- 배포 파일을 `random-roulette.v3.1.*`로 올리고 서비스 워커 캐시 키를 갱신해 기존 캐시 영향을 차단했다.
- 실제 브라우저에서 시작 → 참가자 명단 열기 → 입력창 클릭·3줄 입력 → 닫기 → 운 메뉴 이동 순서로 재검사했다.
- 입력 중 계산 스타일 `cursor: text`, 커스텀 커서 투명도 `0`, 포인터/팝업 레이어 `10080/10020`, 앱 스크립트 오류 `0건`을 확인했다.

## 이번 수정

### 2026-08 모듈·기본 룰렛·PWA 개편

- 기본 원판 룰렛에 항목별 가중치, 재추첨, 당첨자 제거, 자동 제거, 최근 기록을 추가했다.
- 공용 참가자 명단을 한 번 저장하면 모든 게임 입력 형식에 맞게 자동 반영한다.
- 랜덤 게임 뽑기는 현재 인원수와 기기에서 실행 가능한 게임만 선택한다.
- 진행 중인 게임에서 화면 이동 또는 리셋할 때만 확인 대화상자가 나타난다.
- 주식 계산은 모든 기기에서 250ms 고정, DOM 렌더링만 성능 등급에 따라 250~500ms로 제한한다.
- 담아라! 구슬 수와 Matter.js 물리 시간축을 기기별로 동일하게 맞췄다.
- 기존 폭격 해전 중복 구현을 배포 번들에서 제거하고 게임별 소스 모듈과 자동 빌드를 추가했다.
- PWA 설치, 오프라인 핵심 화면, 게임 진행 중 Screen Wake Lock을 추가했다.
- 정적·동적 DOM에서 미지원 이모지를 감지하여 대체 기호로 자동 교체한다.

### 담아라! 화면 이탈 후 구슬 소리 지속 버그 수정

- `담아라!` 게임 중 메뉴/이전 화면으로 이동하면 기존 구슬 생성 타이머와 물리 충돌 이벤트가 남아 있어 구슬 낙하음·충돌음이 계속 들릴 수 있었다.
- `showScreen()`에서 `game1` 화면을 벗어날 때 `stopGame1LiveRound()`를 호출하도록 변경했다.
- `stopGame1LiveRound()`는 다음 항목을 정리한다.
  - 구슬 생성 타이머
  - 카운트다운/정착/최종 감시 타이머
  - 남아 있는 구슬/폭탄 바디
  - 게임1 입력/셔플 잠금 상태
- `handleGame1CollisionAudio()`에 현재 화면이 `담아라!`일 때만 충돌음을 재생하는 가드를 추가했다.
- 구슬 생성 예약 콜백에도 세션 토큰과 활성 화면 검사를 추가해 화면 이탈 후 예약 생성이 이어지지 않게 했다.

## 정적 검수 결과

- `node --check script.js`: 통과
- `node --check script.min.js`: 통과
- 음량 슬라이더 독립 UI 동기화 및 루트 배포 번들 일치 검사: 통과
- HTTP 기준 `index.html`, CSS, 음량 UI, 메인 JS, manifest, service worker 응답 확인: 통과
- 실제 브라우저 시작→메뉴, 모든 운 게임·현재 명단 랜덤 선택, 데스크톱 실행 가능 피지컬 게임 진입·복귀: 통과
- 기본 룰렛 회전·결과·기록·제거·재추첨, 진행 중 이동 확인·취소·정상 복귀: 통과
- 7개 운 게임 시작·리셋, 풍선·STAY CLICK·곰찾기 시작·리셋, 셔플·전체화면·테마·사운드·공용 명단: 통과
- 배경음 63%, 효과음 27% 변경 시 퍼센트와 채움 색상 동기화: 통과
- HTML이 참조하는 로컬 CSS·JS·Matter.js·영상·이미지 파일 존재 확인
- PC/모바일 영상의 H.264 + AAC, yuv420p 호환성 확인
- 모바일, 일반 PC, 저사양 PC 환경 모의 실행에서 모든 화면 진입·이탈 스모크 테스트 통과
- 세 환경 모두 최종 홈 화면 복귀 및 콘솔 예외 0건 확인

## 유지된 사항

- 게임 로직의 당첨/집계 방식은 변경하지 않았다.
- 기존 음향 튜닝, 풍선 차례 색상, 밤모드 경마 색상, 운 게임 메뉴 8개 제한 수정은 유지했다.

## 2026-07 볼 배틀 성능 수정

- Matter.js `afterUpdate` 이벤트와 명시적 강제 갱신을 구분해 HP 라벨이 물리 프레임마다 강제 갱신되던 문제를 수정했다.
- 모바일·사무용 PC 캔버스 렌더링을 약 30fps로 제한하고 픽셀 비율을 각각 0.7·0.85로 낮췄다.
- 일반 PC도 픽셀 비율을 1.1로 제한하고 렌더 루프를 물리 루프에서 분리했다.
- 안전 구역 시각 DOM 쓰기, HP 라벨 이동, 전투 문구, 랭킹 갱신을 각각 독립적으로 제한했다.
- 피격 효과의 `offsetWidth` 강제 레이아웃을 제거하고, 모바일에서는 떠오르는 데미지 DOM을 만들지 않는다.
- 모바일·저사양 PC에서는 라벨의 백드롭 블러와 폭발·후반전의 큰 그림자 합성을 제거했다.
- 최종 승자가 정해지면 물리 러너와 연속 캔버스 렌더링이 정지하며, 종료 후 테마 변경은 단일 프레임으로 다시 그리도록 확인했다.
- 모바일·저사양 PC·일반 PC 모의 전투에서 캔버스 1개, 물리 공 2개, HP 오버레이 2개가 정상 생성·정리됐다.
- 0.6초 동안 12회 연속 충돌 모의 실행 시 세 환경 모두 랭킹 전체 갱신 1회로 병합됐고, 모바일 일회성 데미지 DOM은 0개였다.

## 성능 회귀 방지

- 홈 화면 진입 시 숨겨진 해전·주식·사다리·풍선 UI를 미리 렌더링하지 않는다.
- 화면을 실제로 떠날 때만 해당 게임의 타이머와 상태를 정리한다.
- 담아라! 물리엔진은 화면 이탈 또는 백그라운드 탭에서 정지하고 재진입 시 재개한다.
- 곰찾기 영상은 사용자가 상자를 누르기 전에는 내려받지 않는다.

## 2026-07 커서·테마 배경 회귀 수정

- 사무용 PC 성능 모드에서 커스텀 커서는 만들지 않으면서 기본 커서만 숨기던 조건 충돌을 수정했다.
- 커스텀 커서 요소가 준비되고 실제 마우스 입력이 들어온 뒤에만 기본 커서를 숨긴다.
- 모바일·사무용 성능 모드가 낮/밤 테마의 기존 배경을 공통 배경으로 덮어쓰던 규칙을 제거했다.
- 낮/밤 테마의 색상·그라데이션·장식 배경은 유지하고, 성능 모드에서는 장식의 반복 애니메이션만 정지한다.

## 2026-08 PC 모바일 게임 노출·이모지 호환성 재수정

- 모바일 전용 게임의 판정을 터치 포인터 중심에서 운영체제·브라우저 식별값 우선으로 변경했다.
- 터치 화면, 작은 창, 태블릿 모드를 쓰는 Windows PC도 데스크톱으로 고정 판정한다.
- Android 태블릿과 iPadOS 데스크톱 UA는 모바일 기기로 판정해 모바일 전용 게임을 유지한다.
- CSS는 모바일 전용 카드를 PC에서 기본적으로 숨기고, JavaScript가 모바일 OS를 확인해 `catalog-handheld-device`를 부여한 경우에만 표시한다.
- 이모지 누락 검사는 `□`·대체문자뿐 아니라 미할당 코드포인트 3종의 실제 캔버스 픽셀 서명과 비교하도록 보강했다.
- 최근 추가되어 OS별 누락이 잦은 이모지는 Windows·Android·Linux·구형 WebView에서 즉시 안전 기호로 교체한다.
- 모든 화면·동적 게임 UI에서 사용하는 이모지에 대체표가 있으며, 각 항목의 마지막 후보는 반드시 ASCII 문자임을 자동 검사한다.
- 배포 파일을 `random-roulette.v3.5.*`로 올리고 서비스 워커 캐시 키를 갱신했다.
- 자동 검사는 터치 Windows PC·일반 macOS를 PC로, Android 태블릿·iPadOS 데스크톱 UA를 모바일로 판정하는 4개 시나리오를 모두 통과했다.
- 실제 PC 브라우저에서 시작→랜덤 게임 목록 진입 후 원본 카드 14개 중 12개만 표시되고 모바일 전용 2개는 `hidden` 및 `display:none`인 것을 확인했다.
- 이모지 강제 호환 모드의 홈·동적 게임 목록에서 확장 이모지와 대체문자 `�`가 모두 0개이며 앱 출처 콘솔 오류가 0건임을 확인했다.

## 2026-08 게임 카드 기기 배지 제거

- 게임 아이콘을 가리던 `모바일 · PC`, `모바일 전용`, `PC 전용` 카드 배지 5개를 제거했다.
- 기기 호환 판정용 `data-physical-compat` 속성은 유지해 PC·모바일별 게임 숨김 동작에는 영향이 없다.
- 배지를 위해 추가했던 피지컬 카드의 별도 상단 여백을 제거하고, 모든 카드가 빨리감기 배지를 기준으로 같은 상단 정렬을 사용하도록 통일했다.
- 실제 PC 브라우저에서 배지 요소 0개, 표시 중인 피지컬 카드 아이콘과 빨리감기 배지의 겹침 0건, 모바일 전용 카드 2개 숨김, 앱 출처 콘솔 오류 0건을 확인했다.
- 배포 파일을 `random-roulette.v3.6.*`로 올리고 서비스 워커 캐시를 갱신했다.
