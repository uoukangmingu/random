# Random Roulette 컬러/가시성 재검수 보고서

## 적용 수정

1. 낮 모드 재정의
   - 낮의 푸른 하늘 아래에 있는 느낌으로 배경을 하늘색 그라데이션과 부드러운 구름 톤으로 재구성했다.
   - 카드/버튼/게임 요소는 흰색과 파스텔톤을 기본으로 유지하되, 텍스트는 진한 블루그레이 계열로 고정해 대비를 높였다.
   - 핑크, 옐로, 민트, 라벤더는 강조 요소에만 사용해 눈부심을 줄였다.

2. 밤 모드 재정의
   - 어두운 밤 배경, 별빛, 하단 네온 라인, 네온 사인처럼 은은하게 빛나는 강조색을 적용했다.
   - 본문 텍스트는 거의 흰색에 가까운 블루 화이트, 보조 텍스트는 낮은 채도의 블루그레이로 정리했다.
   - 네온 효과는 테두리와 버튼 중심으로 제한해 눈이 아프지 않도록 강도를 낮췄다.

3. 가독성/구분감 개선
   - 카드, 입력창, 버튼, 게임 목록, 팝업, 랭킹/상태 칩, 스테이지 영역의 배경과 테두리를 낮/밤 각각 다르게 분리했다.
   - 텍스트와 배경 대비가 낮아지는 연한 파스텔 조합은 줄이고, 텍스트 색상은 명도 차이가 큰 색으로 고정했다.
   - 기능 레이아웃과 JS 로직은 수정하지 않았다.

## 정적 검수 결과

- `node --check script.js`: 통과
- HTML 중복 ID: 없음
- CSS는 레이아웃 변경 없이 색상/배경/테두리/그림자 중심으로만 오버라이드했다.

## 남은 리스크

1. 실제 모바일 기기별 화면 밝기 차이
   - OLED/저가형 LCD/자동 밝기 환경에 따라 밤 모드 네온 강도가 다르게 보일 수 있다.
   - 실제 기기에서 1차 밝기 테스트가 필요하다.

2. Matter.js CDN 의존
   - `담아라!`, `볼 배틀`은 Matter.js가 필요하다.
   - CDN이 막히면 게임 자체가 실행되지 않는다.
