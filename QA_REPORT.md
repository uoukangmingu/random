# Random Roulette 사용성/레이아웃 재검수 보고서

## 이번 추가 수정

1. STAY CLICK 설정 패널 겹침 수정
   - 스크린샷의 빨간 영역처럼 시작/리셋 버튼이 아래 참가자 현황 카드에 눌려 보이는 문제를 수정했다.
   - 원인은 STAY CLICK 전용 compact 모드에서 왼쪽 설정 패널을 `overflow: hidden`으로 막아둔 상태에서, 참가자 입력/키 지정/버튼/상태문/참가자 현황이 한 높이 안에 모두 들어가지 못하는 구조였다.
   - 설정 패널 내부 스크롤을 허용하고, 키 지정 목록/참가자 현황 목록만 제한 높이 안에서 스크롤되도록 조정했다.

2. STAY CLICK 복귀 동선 추가
   - STAY CLICK 화면 오른쪽 상단에 `← 피지컬 목록으로` 버튼을 추가했다.
   - 기존에는 우측 하단 유틸리티의 `이전으로`에 의존해야 해서, 게임 화면 자체 안에서 빠져나가는 동선이 약했다.

3. 노트북/중간 폭 화면 유틸리티 간섭 완화
   - 게임 화면에서 우측 하단 밤모드/전체화면/사운드/이전으로 컨트롤이 화면을 덮을 가능성을 줄였다.
   - 1025~1520px 폭에서는 게임 중 유틸리티 버튼을 아이콘형으로 축소하고 볼륨 패널은 숨기도록 조정했다.

4. 낮은 높이 화면 대응
   - 760px 이하 높이의 데스크톱/노트북에서 STAY CLICK 제목, 설명, 스테이지 여백을 줄여 핵심 조작부가 먼저 보이도록 조정했다.

## 정적 검수 결과

- `node --check script.js`: 통과
- HTML 중복 ID: 없음
- STAY CLICK 복귀 버튼 1개 추가 확인
- JS 게임 로직은 변경하지 않고 HTML/CSS 레이아웃만 수정

## 남은 리스크

1. 실제 브라우저/기기별 높이 차이
   - 크롬 주소창, 윈도우 작업표시줄, 브라우저 확대율에 따라 768px급 화면에서는 내부 스크롤이 발생할 수 있다.
   - 이번 수정은 요소가 가려지는 것보다 스크롤로 접근 가능하게 만드는 방향이다.

2. Matter.js CDN 의존
   - `담아라!`, `볼 배틀`은 Matter.js가 필요하다.
   - CDN이 막히면 해당 물리 게임은 실행되지 않는다.

## 2026-06-10-3 추가 수정

- 밤모드의 박스 모델을 낮모드 기준으로 통일했다.
- 게임 카드, 메뉴 카드, 버튼, 배지, 입력창, 게임 스테이지, 랭킹/현황 카드의 border 두께와 핵심 아이콘 크기를 낮모드와 맞췄다.
- 색상 팔레트는 밤모드를 유지하되, 요소 크기·간격·레이아웃이 테마 전환으로 흔들리지 않도록 CSS 하단에 전용 패리티 패치를 추가했다.
- 기능 JS 로직은 수정하지 않았다.
