# Random Roulette QA Report

## 이번 수정

### 담아라! 화면 이탈 후 구슬 소리 지속 버그 수정

- `담아라!` 게임 중 메뉴/이전 화면으로 이동하면 기존 구슬 생성 타이머와 물리 충돌 이벤트가 남아 있어 구슬 낙하음·충돌음이 계속 들릴 수 있었다.
- `showScreen()`에서 `game1` 화면을 벗어날 때 `stopGame1LiveRound()`를 호출하도록 변경했다.
- `stopGame1LiveRound()`는 다음 항목을 정리한다.
  - 구슬 생성 타이머
  - 카운트다운/정착/최종 감시 타이머
  - 남아 있는 구슬/폭탄 바디
  - 게임1 입력/셔플 잠금 상태
- `handleGame1CollisionAudio()`에 현재 화면이 `담아라!`일 때만 충돌음을 재생하는 가드를 추가했다.
- 구슬 생성 예약 콜백에도 세션 토큰과 활성 화면 검사를 추가해 화면 이탈 후 예약 생성이 이어지지 않게 했다.

## 정적 검수 결과

- `node --check script.js`: 통과
- `node --check script.min.js`: 통과
- HTML이 참조하는 로컬 CSS·JS·Matter.js·영상·이미지 파일 존재 확인
- PC/모바일 영상의 H.264 + AAC, yuv420p 호환성 확인
- 모바일, 일반 PC, 저사양 PC 환경 모의 실행에서 모든 화면 진입·이탈 스모크 테스트 통과
- 세 환경 모두 최종 홈 화면 복귀 및 콘솔 예외 0건 확인

## 유지된 사항

- 게임 로직의 당첨/집계 방식은 변경하지 않았다.
- 기존 음향 튜닝, 풍선 차례 색상, 밤모드 경마 색상, 운 게임 메뉴 8개 제한 수정은 유지했다.

## 2026-07 볼 배틀 성능 수정

- Matter.js `afterUpdate` 이벤트와 명시적 강제 갱신을 구분해 HP 라벨이 물리 프레임마다 강제 갱신되던 문제를 수정했다.
- 모바일·사무용 PC 캔버스 렌더링을 약 30fps로 제한하고 픽셀 비율을 각각 0.7·0.85로 낮췄다.
- 일반 PC도 픽셀 비율을 1.1로 제한하고 렌더 루프를 물리 루프에서 분리했다.
- 안전 구역 시각 DOM 쓰기, HP 라벨 이동, 전투 문구, 랭킹 갱신을 각각 독립적으로 제한했다.
- 피격 효과의 `offsetWidth` 강제 레이아웃을 제거하고, 모바일에서는 떠오르는 데미지 DOM을 만들지 않는다.
- 모바일·저사양 PC에서는 라벨의 백드롭 블러와 폭발·후반전의 큰 그림자 합성을 제거했다.
- 최종 승자가 정해지면 물리 러너와 연속 캔버스 렌더링이 정지하며, 종료 후 테마 변경은 단일 프레임으로 다시 그리도록 확인했다.
- 모바일·저사양 PC·일반 PC 모의 전투에서 캔버스 1개, 물리 공 2개, HP 오버레이 2개가 정상 생성·정리됐다.
- 0.6초 동안 12회 연속 충돌 모의 실행 시 세 환경 모두 랭킹 전체 갱신 1회로 병합됐고, 모바일 일회성 데미지 DOM은 0개였다.

## 성능 회귀 방지

- 홈 화면 진입 시 숨겨진 해전·주식·사다리·풍선 UI를 미리 렌더링하지 않는다.
- 화면을 실제로 떠날 때만 해당 게임의 타이머와 상태를 정리한다.
- 담아라! 물리엔진은 화면 이탈 또는 백그라운드 탭에서 정지하고 재진입 시 재개한다.
- 곰찾기 영상은 사용자가 상자를 누르기 전에는 내려받지 않는다.

## 2026-07 커서·테마 배경 회귀 수정

- 사무용 PC 성능 모드에서 커스텀 커서는 만들지 않으면서 기본 커서만 숨기던 조건 충돌을 수정했다.
- 커스텀 커서 요소가 준비되고 실제 마우스 입력이 들어온 뒤에만 기본 커서를 숨긴다.
- 모바일·사무용 성능 모드가 낮/밤 테마의 기존 배경을 공통 배경으로 덮어쓰던 규칙을 제거했다.
- 낮/밤 테마의 색상·그라데이션·장식 배경은 유지하고, 성능 모드에서는 장식의 반복 애니메이션만 정지한다.
