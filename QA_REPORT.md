# Random Roulette 사용성/레이아웃 재검수 보고서

## 2026-06-10-7 추가 수정

1. STAY CLICK 복귀 버튼 제거
   - STAY CLICK 화면 상단의 `← 피지컬 목록으로` 버튼을 제거했다.
   - 우측/하단 공통 뒤로가기 동선이 이미 존재하므로 중복 버튼을 없앤다.

2. 풍선 불기 정보 노출 제거
   - 풍선이 언제 터질지 예측할 수 없도록 현재 압력 표시 바를 제거했다.
   - 풍선 내부의 숫자/퍼센트 표시를 상태 문구로 변경했다.
   - 72% 이상에서 재생되던 `balloonWarning` 효과음 호출을 제거했다.
   - 경고/위험 단계의 시각적 강조 효과도 비활성화했다.

3. 작아지는 원 난이도 조정
   - 게임 시작 시 원이 박스 안 랜덤 위치에 배치된다.
   - 성공 터치 후 원이 작아지면서 다시 박스 안의 다른 랜덤 위치로 이동한다.
   - 원이 박스 밖으로 나가지 않도록 현재 원 반지름과 스테이지 크기를 기준으로 이동 범위를 제한했다.

## 정적 검수 결과

- `node --check script.js`: 통과
- STAY CLICK 전용 `key-react-back-btn`: 제거 확인
- 풍선 불기 `현재 압력` 표시 영역: 제거 확인
- `playThrottledSfx('balloonWarning', ...)`: 제거 확인
- 운 게임 메뉴 반복 표시 방지 패치 유지
- 이미지/영상 에셋은 `assets/` 폴더에 포함됨

## 남은 리스크

1. 실제 모바일 터치감
   - 작아지는 원의 랜덤 이동은 기기 화면 크기와 브라우저 UI 높이에 따라 체감 난이도가 달라질 수 있다.
   - 너무 어렵다고 느껴지면 `CIRCLE_TAP_MIN_RANDOM_MOVE_RATIO` 또는 `CIRCLE_TAP_EDGE_PADDING` 값을 조정하면 된다.

2. 풍선 게임의 긴장감
   - 압력 바와 경고음은 제거했지만, 풍선 크기 변화 자체는 여전히 남아 있다.
   - 완전히 예측 불가능하게 만들려면 풍선 크기 증가량을 더 비선형으로 조정할 수 있다.

3. Matter.js CDN 의존
   - `담아라!`, `볼 배틀`은 Matter.js가 필요하다.
   - CDN이 막히면 해당 물리 게임은 실행되지 않는다.
