# 랜덤 룰렛 v3.29 검증 기록

검증일: 2026-09-10. v3.28을 바탕으로 볼 배틀의 렌더링, 효과, 반응형 화면과 조작을 개선했습니다.

## 수정 내용

- 공을 그리는 캔버스와 이름표의 갱신 주기가 달라 생기던 어긋남을 제거했습니다. 물리와 표시를 하나의 프레임 루프에서 처리하고, 물리는 60Hz 고정 간격으로 계산합니다. 화면에는 이전·현재 위치 사이를 보간해 표시합니다.
- 이름표의 transform 전환 지연과 모바일 음수 왼쪽 여백을 제거했습니다. 위치를 경기장 안으로 제한하고, 이름/체력에 12px 글자와 참가자 색 테두리를 적용했습니다.
- 매 충돌마다 순위 전체를 다시 생성하던 동작을 참가자별 DOM 재사용과 변경 항목 갱신으로 바꿨습니다. 남은 HP 숫자와 위험 체력을 표시합니다.
- 충돌 링·피해 숫자·폭발 파동을 캔버스에서 재사용합니다. 효과 풀은 20개, 동시 활성 효과는 화질에 따라 6/10/18개로 제한됩니다. 저성능 모드에서는 숫자 효과를 줄이되 공과 충돌 피드백을 유지합니다.
- 폭탄의 예고·제거를 물리 시계로 관리해 일시정지나 리셋 뒤 남은 타이머가 다른 경기에 작용하지 않게 했습니다. 종료 시 일시적인 효과도 정리합니다.
- 방향 전환과 폭발 후 속도 복귀를 완만하게 처리했습니다. 3배속은 큰 물리 간격 대신 작은 계산을 반복하므로 충돌의 시간 간격은 유지됩니다. 긴 프레임의 따라잡기 계산량도 제한했습니다.
- 일시정지/계속하기, 맵·생존 수·경과 시간·후반전 상태를 경기장 가까이에 표시합니다. 반복 충돌 문구를 스크린리더가 계속 읽지 않도록 실시간 알림을 끄고, 결과는 기존 결과 대화상자로 안내합니다.
- PC에서는 순위를 경기장 옆에, 작은 화면에서는 경기장 다음에 배치했습니다. 스탯 표는 키보드로 접근하고 가로로 스크롤할 수 있습니다. 320px 화면에서도 경기 버튼 3개가 한 줄에 배치되고 각 높이는 44px입니다.
- 좁은 PC 창에서도 화면 폭에 맞게 설정 화면을 재배치합니다. 모바일 버튼을 aria-hidden 영역에 넣던 문제도 수정했습니다.
- 정지 중 테마/화질 변경으로 캔버스가 비워지는 문제를 수정했습니다. 해상도가 실제로 바뀔 때만 재설정하며, 정지/종료 상태에서는 즉시 다시 그립니다. 이름표와 순위 색도 함께 갱신합니다.
- 맵 선택 중 리셋·이탈한 뒤 이전 선택이 새 경기를 시작하지 않도록 실행 토큰을 확인합니다.

## 실행 검증

`npm run check` 전체 회귀 검사를 통과했습니다. 마지막 정지 중 테마/화질 보완 후에는 `node scripts/qa-ball-battle-runtime.mjs`와 최종 번들 구문 검사를 다시 통과했습니다.

볼 배틀 검사는 원본 script.js의 실제 함수를 추출하고 포함된 Matter 엔진을 실행합니다. 시간·DOM·캔버스 명령·소리는 검사 환경으로 대체하므로 아래 수치는 기기 FPS 측정값이 아닙니다.

| 가상 화면 갱신률 | 10초 동안 물리 계산 | 확인 |
| --- | --- | --- |
| 30Hz | 600회 | 기준 경로 |
| 60Hz | 600회 | 같은 경로 |
| 120Hz | 600회 | 같은 경로 |
| 144Hz | 600회 | 같은 경로 |

- 경로 비교 허용 오차: 1e-7. 이름표와 캔버스가 같은 보간 위치를 사용합니다.
- 수동 정지 30초, 비활성 탭 60초 동안 물리 시간이 진행되지 않았습니다. 복귀를 반복해도 프레임 루프가 중복되지 않았고 수동 정지가 유지됐습니다.
- 3배속의 1초는 물리 시간 3초로 처리되며 개별 계산 간격은 1/60초를 넘지 않았습니다.
- 반복 효과에서 DOM을 생성하지 않으며 활성 효과 상한, 순위 노드 재사용, 리셋 후 재시작, 종료 후 프레임/효과 정리를 확인했습니다.
- 정지 중 테마 변경과 캔버스 해상도 변경에서 다시 그려짐을 확인했습니다. 동일 해상도 재적용은 캔버스를 다시 비우지 않습니다.

| 경기장 | 폭 320 / 낮은 품질, 6명 | 폭 1000 / 높은 품질, 6명 |
| --- | --- | --- |
| 클래식 | 43초 종료 | 64초 종료 |
| 폭탄 창고 | 29초 종료 | 66초 종료 |
| 회전 막대 | 45초 종료 | 37초 종료 |
| 핀볼 링 | 38초 종료 | 56초 종료 |

위 종료 시간은 고정 난수 조건의 물리 시간입니다. 모든 경우 1명 생존, 1~6위 중복 없음, 좌표 유효성, 종료 후 프레임 정지를 확인했습니다.

기존 원판 0.5~10배 속도와 회전 중 배율 변경, SPIN/STOP, 모션 감소 설정, 카드 후반 공개 순서, 담아라! 시작·복귀, 게임 목록, 경마, 배경음·효과음 검사를 유지했습니다.

## 화면 확인 범위

- 데스크톱 브라우저에서 6명 등록, 스탯 공개, 폭탄 경기장 진행, HP 변경, 일시정지/계속하기 표시와 확대/복원 화면을 확인했습니다.
- 동일 브라우저의 390px·320px 반응형 프레임에서 회전 막대와 핀볼 경기장을 확인했습니다. 가로 넘침이 없으며, 320px에서 세 조작 버튼이 같은 높이 44px로 한 줄에 배치됩니다.
- 모바일 폭에서 이름표의 음수 여백 제거, 경기장 크기 변경과 확대, 정지 유지, 낮·밤 안내문 가독성을 확인했습니다.
- 이 과정에서 발견한 정지 중 테마/화질 캔버스 소실을 수정하고 실제 물리 엔진 기반 회귀 검사로 확인했습니다. 마지막 테마 전환 후 추가 스크린샷은 세션 중단으로 수집하지 않았습니다.
- 실제 iPhone/Android 기기, 사무용 PC의 FPS·터치 지연·배터리 사용량은 측정하지 않았습니다. 특정 기기의 무지연이나 고정 FPS를 보장하는 수치는 아닙니다.

## 배포 파일

이 ZIP은 index.html, v3.29 실행 번들, 모든 기존 자산과 소스/빌드 스크립트를 포함합니다. 이전 버전 실행 번들, 개발 의존성 폴더, 로컬 화면 검사 페이지는 제외했습니다. 최상위 random-roulette-v3.29 폴더 안의 파일을 정적 호스팅에 올릴 수 있습니다.

초기 코드 96,003바이트는 v3.28과 같습니다. 게임 JavaScript 486,597바이트, CSS 590,560바이트이며 정확한 압축 크기는 PERFORMANCE_COMPARISON.json을 참조하세요.
